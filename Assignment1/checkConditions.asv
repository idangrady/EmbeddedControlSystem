function [bool] = checkConditions(x_, u,states, cond_state, cond_input)
bool = false;
for i=1:length(x_)
cur_state = 
end
    if(or(x_(3)>50 , x_(4)>cond_state))
        if(x_(3)>50)
         disp('state 4 constraint failed')
        end
        if(x_(4)>cond_state)
         disp('state 4 constraint failed')
        end
    bool = true;
    end
    if(abs(u)>-cond_input)
     disp('input constraint failed')
     bool = true;
    end    
end